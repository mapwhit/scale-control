var should = require('should');
var mapScaleControl = require('../');

describe('map-scale-control node module', function () {
  it('must have at least one test', function () {
    mapScaleControl();
    should.fail('Need to write tests.');
  });
});
